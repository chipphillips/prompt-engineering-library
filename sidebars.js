// @ts-check

/** @type {import('@docusaurus/plugin-content-docs').SidebarsConfig} */
const sidebars = {
  promptSidebar: [
    {
      type: 'category',
      label: 'Introduction',
      items: ['intro'],
    },
    // More categories will be added based on your mind map
  ],
};

export default sidebars;
